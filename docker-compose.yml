services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: connect4
      POSTGRES_PASSWORD: connect4
      POSTGRES_DB: connect4
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
